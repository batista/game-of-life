const assert = require('assert');
const gameOfLife = require('../index.js');

describe('gameOfLife', function () {
  it('has a test', function () {
    assert(false, 'gameOfLife should have a test');
  });
});
